<%= form_for(@inspection_requirement) do |f| %>
<% if @inspection_requirement.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(@inspection_requirement.errors.count, "error") %> prohibited this inspection_requirement from being saved:</h2>

  <ul>
    <% @inspection_requirement.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
</div>
<% end %>


<div class="field">
  <%= f.label :inspection %><br>
  <%= collection_select(:inspection_requirement, :inspection_id, Inspection.all, :id, :nombre, {:prompt => false}) %> 
</div>

<div class="field">
  <%= f.label  :requirement%><br>
  <%= collection_select(:inspection_requirement, :requirement_id, Requirement.all, :id, :nombre, {:prompt => false}) %> 
</div>
<div class="actions">
  <%= f.submit %>
</div>
<% end %>
