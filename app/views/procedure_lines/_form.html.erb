<%= form_for(@procedure_line) do |f| %>
<% if @procedure_line.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(@procedure_line.errors.count, "error") %> prohibited this procedure_requirement from being saved:</h2>

  <ul>
    <% @procedure_line.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
</div>
<% end %>


<div class="field">
  <%= f.label :procedure %><br>
  <%= collection_select(:procedure_line, :procedure_id, Procedure.all, :id, :nombre, {:prompt => false}) %> 
</div>

<div class="field">
  <%= f.label  :line%><br>
  <%= collection_select(:procedure_line, :line_id, Line.all, :id, :nombre, {:prompt => false}) %> 
</div>
<div class="actions">
  <%= f.submit %>
</div>
<% end %>
