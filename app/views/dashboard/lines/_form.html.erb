<div id="form-container">
  <%= form_for [:dashboard, @lines], html: { class: 'form-horizontal' } do |f| %>
    <% if @lines.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@lines.errors.count, "error") %> prohibited this lines from being saved:</h2>

        <ul>
        <% @lines.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <div class="form-group">
      <%= f.label :name, class: 'col-sm-2 control-label' %>
      <div class="col-sm-10">
        <%= f.text_field :name, class: 'form-control' %>
      </div>
    </div>

    <div class="form-group">
      <%= f.label :description, class: 'col-sm-2 control-label' %>
      <div class="col-sm-10">
        <%= f.text_field :description, class: 'form-control' %>
      </div>
    </div>


    <div class="form-group">
      <%#= f.label :dependencia, class: 'col-sm-2 control-label' %>
      <div class="col-sm-10">
        <%#= collection_select(:inspection, :dependency_id, Dependency.all, :id, :nombre, {:prompt => false}, class: 'form-control') %>
      </div>
    </div>

    <div class="actions">
      <%= f.submit t('.create_formation_steps'), class: 'btn btn-default pull-right' %>
    </div>
  <% end %>
</div>